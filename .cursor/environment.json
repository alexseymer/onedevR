{
  "name": "onedevr",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "Rscript -e 'if (!file.exists(\"DESCRIPTION\")) quit(save = \"no\", status = 0); remotes::install_deps(\".\", dependencies = TRUE, upgrade = \"never\")'"
}
