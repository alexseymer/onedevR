# Normalize a OneDev API collection payload to a list of items

Accepts bare arrays, `{items: [...]}`, `{data: [...]}`, or a single
object with an `id` field.

## Usage

``` r
.od_normalize_collection(response)
```
