# Set custom issue fields

Set custom issue fields

## Usage

``` r
od_issue_set_fields(issue_number, fields, conn = NULL)
```

## Arguments

- issue_number:

  UI number.

- fields:

  Named list of field values (installation-specific schema).

- conn:

  Connection list.

## Value

Parsed API response.
