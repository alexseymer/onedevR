# Resolve a build UI number to internal id for artifact calls

Resolve a build UI number to internal id for artifact calls

## Usage

``` r
.od_artifact_build_id(build_number, conn, use_internal_id = FALSE)
```
