# Merge a pull request

Merge a pull request

## Usage

``` r
od_merge_pull_request(pull_request_number, conn = NULL)
```

## Arguments

- pull_request_number:

  UI number.

- conn:

  Connection list.

## Value

Parsed API response (may be `NULL`).
