# Normalize job params to OneDev's map-of-string-lists shape

Accepts a named list/vector where each element is a character scalar or
vector of values.

## Usage

``` r
.od_job_params(params = NULL)
```
