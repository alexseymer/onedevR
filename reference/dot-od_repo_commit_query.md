# Attach repeated `field` query params for commit endpoints

Attach repeated `field` query params for commit endpoints

## Usage

``` r
.od_repo_commit_query(query = NULL, count = NULL, fields = NULL)
```
