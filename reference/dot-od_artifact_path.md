# Normalize an artifact path for OneDev artifact endpoints

Normalize an artifact path for OneDev artifact endpoints

## Usage

``` r
.od_artifact_path(artifact_path = NULL, leading_slash = FALSE)
```
