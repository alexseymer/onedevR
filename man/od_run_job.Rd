% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jobs.R
\name{od_run_job}
\alias{od_run_job}
\title{Run a CI/CD job}
\usage{
od_run_job(
  job_name,
  project = NULL,
  branch = NULL,
  tag = NULL,
  commit_hash = NULL,
  ref_name = NULL,
  pull_request_id = NULL,
  params = NULL,
  reason = "Submitted via onedevr",
  conn = NULL
)
}
\arguments{
\item{job_name}{Job name from the project build spec.}

\item{project}{Project path or id; defaults to the connection project.}

\item{branch}{Branch name (e.g. \code{"main"}) - mutually exclusive with \code{tag},
\code{commit_hash}, and \code{pull_request_id}.}

\item{tag}{Tag name - mutually exclusive with \code{branch} / commit / PR modes.}

\item{commit_hash}{Commit SHA to build (requires \code{ref_name}).}

\item{ref_name}{Full git ref (e.g. \code{"refs/heads/main"}) when using
\code{commit_hash}.}

\item{pull_request_id}{Internal pull request id (not the UI number).}

\item{params}{Named list of job parameters (values are character scalars or
vectors for multi-valued params).}

\item{reason}{Reason string recorded on the build (required by OneDev).}

\item{conn}{Connection list from \code{\link[=od_get_config]{od_get_config()}} / \code{\link[=od_connection]{od_connection()}}.}
}
\value{
Internal build id (numeric/character as returned by the API).
}
\description{
Posts to \code{POST /job-runs}. Prefer \code{branch} / \code{tag} (resolves tip commit +
ref), or pass \code{commit_hash} + \code{ref_name} explicitly. For pull-request merge
previews, pass \code{pull_request_id} (internal REST id).
}
\details{
Returns the internal build id (numeric). Use \code{\link[=od_get_build]{od_get_build()}} with
\code{use_internal_id = TRUE} or query by number once known.
}
\examples{
\dontrun{
od_run_job("CI", branch = "main")
}
}
\seealso{
Other builds:
\code{\link[=od_cancel_job]{od_cancel_job()}},
\code{\link[=od_download_build_artifact]{od_download_build_artifact()}},
\code{\link[=od_get_build]{od_get_build()}},
\code{\link[=od_get_build_log]{od_get_build_log()}},
\code{\link[=od_get_build_params]{od_get_build_params()}},
\code{\link[=od_list_build_artifacts]{od_list_build_artifacts()}},
\code{\link[=od_query_builds]{od_query_builds()}},
\code{\link[=od_rebuild_job]{od_rebuild_job()}},
\code{\link[=od_resolve_build_id]{od_resolve_build_id()}}
}
\concept{builds}
